import 'package:flutter/material.dart';
abstract class Styles {
  static const appPadding = Padding(padding: EdgeInsets.only(left: 16));
  static const titleMedium =
      TextStyle(fontWeight: FontWeight.w600, fontSize: 20);
}
